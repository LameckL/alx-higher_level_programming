#!/usr/bin/python3
def magic_calculation(a, b, c):
    """func performing a task similar to that of the given bytecode"""
    if a < b:
        return (c)
    if c > b:
        return (a + b)
    return (a*b - c)
